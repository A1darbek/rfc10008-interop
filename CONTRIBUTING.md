# Contributing

Contributions should add observable evidence, not unverifiable claims.

- Keep target definitions in JSON.
- Keep runner dependencies to the Python 3 standard library.
- Do not commit secrets, cookies, or personal headers.
- Do not make live external network checks required for normal PR validation.
- Mark optional or target-specific behavior as `NOT_SUPPORTED` or
  `NOT_APPLICABLE` rather than `FAIL`.
